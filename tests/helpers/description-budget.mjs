// Claude Code truncates each MCP tool description, and the server instructions,
// at this many characters. Shared so every placement check tracks the same window.
export const CLAUDE_CODE_TEXT_CAP = 2048;
